const fs = require('fs'); const file = 'src/domains/Commerce/checkout/components/cart/cart-sidebar.tsx'; let c = fs.readFileSync(file, 'utf8'); c = c.replace('const [discountPercent, setDiscountPercent] = useState(0);', 'const [discountPercent, setDiscountPercent] = useState(0);\n  const [mounted, setMounted] = useState(false);\n  React.useEffect(() => setMounted(true), []);'); c = c.replace('return (', 'if (!mounted) return null;\n  return ('); fs.writeFileSync(file, c); console.log('Fixed cart hydration');
